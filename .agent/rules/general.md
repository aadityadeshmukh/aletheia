# General Rules for Aletheia

- Always refer to `quartz.config.ts` for theme and component configurations.
- Ensure all content changes are made within the `content/` directory.
- Follow the predefined Quartz layout structure in `quartz.layout.ts`.
- Use descriptive commit messages when committing changes.
